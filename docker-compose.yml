version: '3.8'

services:
  app:
    build: .
    ports:
      - "80:3000"
    volumes:
      - ./data:/app/data      # SQLite DB persists here — back this up!
    env_file:
      - .env
    restart: unless-stopped
